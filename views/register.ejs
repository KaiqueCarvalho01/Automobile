<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro - Automobile</title>
    
    <!-- Os links de CSS e Fonts são os mesmos da página de login -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- Cole aqui o mesmo bloco <style> da página de login.ejs -->
    <style>
        :root {
            --primary-color: #0d6efd;
            --dark-grey: #212529;
            --light-grey: #495057;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-image: 
                linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)),
                url('https://images.unsplash.com/photo-1552519507-da3b142c6e3d?q=80&w=2070&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px 0; /* Adiciona padding vertical para telas menores */
            color: #fff;
        }

        .form-container {
            width: 100%;
            max-width: 450px;
            background: rgba(10, 10, 10, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(15px);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
            animation: fadeIn 0.8s ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .form-container h2 {
            font-weight: 700;
            margin-bottom: 25px;
            text-align: center;
        }
        
        .input-group {
            position: relative;
        }
        
        .form-control {
            background-color: var(--light-grey);
            border: 1px solid transparent;
            color: #fff;
            border-radius: 10px;
            padding: 12px 12px 12px 45px; /* Padding ajustado */
            transition: border-color 0.3s;
        }

        .form-control::placeholder {
            color: #adb5bd;
        }
        
        .form-control:focus {
            background-color: var(--light-grey);
            color: #fff;
            box-shadow: none;
            border-color: var(--primary-color);
        }
        
        .input-group .fas {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #adb5bd;
            z-index: 10;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border: none;
            padding: 12px; /* Padding ajustado */
            border-radius: 10px;
            font-weight: 500;
            transition: background-color 0.3s, transform 0.2s;
        }
        
        .btn-primary:hover {
            background-color: #0b5ed7;
            transform: translateY(-2px);
        }
        
        .form-text-link {
            font-size: 0.9em;
        }
        
        .alert {
            border-radius: 10px;
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h2>Cadastre-se</h2>
        
        <% if (locals.mensagemErro) { %>
            <div class="alert alert-danger"><%= mensagemErro %></div>
        <% } %>

        <form action="/api/users" method="POST" class="mt-4">
            <div class="mb-3 input-group">
                <i class="fas fa-user"></i>
                <input type="text" class="form-control" id="name" name="name" placeholder="Nome Completo" required>
            </div>
            <div class="mb-3 input-group">
                <i class="fas fa-envelope"></i>
                <input type="email" class="form-control" id="email" name="email" placeholder="E-mail" required>
            </div>
            <div class="mb-3 input-group">
                <i class="fas fa-id-card"></i>
                <input type="text" class="form-control" id="cpf" name="cpf" placeholder="CPF (opcional)">
            </div>
            <div class="mb-3 input-group">
                <i class="fas fa-phone"></i>
                <input type="tel" class="form-control" id="telefone" name="telefone" placeholder="Telefone (opcional)">
            </div>
            <div class="mb-3 input-group">
                <i class="fas fa-lock"></i>
                <input type="password" class="form-control" id="password" name="password" placeholder="Crie uma senha" required>
            </div>
            <div class="mb-4 input-group">
                <i class="fas fa-lock"></i>
                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" placeholder="Confirme a senha" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Cadastrar</button>
        </form>

        <p class="mt-4 text-center form-text-link">Já tem uma conta? <a href="/login" class="text-warning fw-bold">Faça Login</a></p>
    </div>

</body>
</html>
